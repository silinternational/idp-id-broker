default:
    suites:
        user_features:
            paths:    [ %paths.base%/features/user ]
            contexts: [ UserContext ]
        password_features:
            paths:    [ %paths.base%/features/password ]
            contexts: [ PasswordContext ]
        authentication_features:
            paths:    [ %paths.base%/features/authentication ]
            contexts: [ AuthenticationContext ]
